# Define a module (like a class or namespace)
defmodule HelloWorld do
  # Define a function inside the module
  def say_hello do
    # Print "Hello, World!" to the console
    IO.puts("Hello, World!")
  end
end

# Call the function to display the output
HelloWorld.say_hello()
