// Here is the "entry" point of every V program
// Each and Every V program must have a `main` function to start execution
fn main() {
    //  `println` function prints the text inside parentheses to the console
    // And It adds a newline also after the message
    println('Hello,World!')
}